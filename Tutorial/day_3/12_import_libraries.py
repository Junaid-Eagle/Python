# want to know the value of pi
import math
import statistics
print("The value of pi is",math.pi)
x = [150,250,350,450]

print(statistics.mean(x))
print(statistics.median(x))
print(statistics.mode(x))
